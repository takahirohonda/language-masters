import clsx from 'clsx'
import { useForm, SubmitHandler } from 'react-hook-form'

export const SignIn = () => {
  return (
    <section className="flex flex-col gap-[8px]">
      <h1 className={clsx(`text-2xl`)}>Sign In</h1>
    </section>
  )
}

{
  "name": "react-voice-recorder",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/react-voice-recorder/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project react-voice-recorder --web",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn nx serve react-voice-recorder"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "yarn nx run react-voice-recorder:lint --fix"
      }
    }
  }
}
